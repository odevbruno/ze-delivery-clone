import React from 'react';
import { View } from 'react-native';
import { CorDisable } from '../../Colors';

export default function Divisor() {
    return (
        <View
            style={{
                height: 1,
                backgroundColor: CorDisable,
                marginTop: 15,
                marginLeft: 15,
                marginRight: 15,
            }}
        />
    );
}